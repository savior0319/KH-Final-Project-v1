<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="myInfo">
	<insert id="question" parameterType="question">
		INSERT INTO QUESTION VALUES(QUESTION_SEQ.NEXTVAL, #{title}, #{content})
	</insert>
	
	<delete id="secessionMember" parameterType="member">
		DELETE FROM MEMBER_TBL WHERE MB_ID = #{mbId}
	</delete>
	
	<update id="updateMyInfo" parameterType="member">
		UPDATE MEMBER_TBL SET MB_PWD = #{mbPwd},,MB_NICKNAME = #{mbNickName}
		WHERE MB_ID = #{mbId}	
	</update>
	
	<select id="selectOneMember">
		SELECT 
		
		FROM WHERE MB_ID = #{mb_id}
	</select>
</mapper>
